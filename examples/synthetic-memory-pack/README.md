# Synthetic memory pack

Synthetic examples only. Do not include real memories, chat logs, coordinates, diaries, or private runtime artifacts.

This pack demonstrates the provider-neutral context boundary:

- `obsidian_context.json` models an Obsidian-shaped note/vault source.
- `hermes_memory_context.json` models a Hermes-memory-shaped source.

Both fixtures describe the same public-safe preference and should feed the same Lumi compatibility packet and Presence decision path without mutating either provider.
